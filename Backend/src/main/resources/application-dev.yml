spring:
  datasource:
    url: jdbc:postgresql://localhost:5431/BookTrack
    username: eliasmilosi
    password: Ursulet2002
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

server:
  port: 8080

logging:
  level:
    com.booktrack: DEBUG
    org.springframework.security: INFO